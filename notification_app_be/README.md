# Notification App Backend

This folder contains a minimal backend scaffold for the notification system.

## Run locally

1. Open a terminal in `notification_app_be`
2. Run `npm install`
3. Set `AUTH_TOKEN` if the upstream API requires authorization.
4. Run `npm start`

The server listens on port `4000` and exposes `/notifications`.
